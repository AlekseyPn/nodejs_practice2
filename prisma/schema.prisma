generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model UserModel {
    id String @id @default(uuid())
    email String
    password String
    name String
}
